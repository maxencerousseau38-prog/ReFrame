# ReFrame V2 — Registre d'architecture (document vivant)

> Règle : tout finding découvert pendant un chantier est **enregistré ici**,
> jamais corrigé hors périmètre. Chaque entrée porte son chantier de
> résolution et la justification du report. Maintenu jusqu'à la fin du projet.
>
> Statuts : `Open` · `In Progress` · `Closed`

## Findings de la revue du Chantier 1 (revue Principal Engineer, 2026-07-03)

| ID | Description | Chantier prévu | Statut | Justification du report |
|----|-------------|----------------|--------|--------------------------|
| **F1** | Granularité des blocs insuffisante sur DOM Framer : la découverte (`snapshot.ts`) ne descend pas les wrappers `<div data-framer-name>` — la géométrie peut se réduire à 1-2 blocs géants sur la plateforme cible n°1. | **C6** (SceneDNA) | **Closed** (2026-07-04, 6a) | Descente géométrique des wrappers (couverture ≥85% page + ≥2 enfants → les enfants concourent ; chaînes mono-enfant ≥90% traversées) + indice mécanique `[data-framer-name]` ; prouvé par la fixture Framer-like (section sous double wrapper + div nu retrouvés, wrappers exclus). |
| **F2** | Sérialisabilité de `collectSnapshot` sous bundler de production non vérifiée (testée sous vitest uniquement). Casse silencieuse possible (`computedSnapshot:false` en prod). | **C10** (premier run prod) | Open | Non observable avant un build/run de prod ; si fragile → migration du script vers une chaîne littérale figée. |
| **F3** | `cssPath` tronqué à 12 ancêtres : collisions possibles sur DOM profonds, path non interrogeable depuis la racine. C6 (diffs responsive) et C10 (patch ciblé) en dépendent. | **C6** | **Closed** (2026-07-04, 6a) | Cap relevé à 16 ancêtres, préfixe « … > » explicite si tronqué (jamais confondu avec un sélecteur requêtable), registre d'unicité par snapshot (suffixe ~n) + cache par élément (même élément = même identité, jointure blocks↔nodes préservée) ; dédoublonnage des nodes imbriqués. |
| **F4** | Buffers de screenshots dans `RenderedSite` : explosion en cas de `JSON.stringify` (route, cache, journal) ; pic Chromium ~66 Mo/screenshot fullPage. | **C4/C10** | **Closed** (décision, C4) | Décision actée : les screenshots restent **memory-only** — aucun chemin de sérialisation ne doit transporter les Buffers ; MEASURE (C4) les ignore, VERIFY (C10) les lira in-process. Toute future API/persistance devra passer par un résumé sans pixels ; réouvrir si C10 exige une persistance. |
| **F5** | Aucun plafond de concurrence des captures : N requêtes = N contextes × 3 screenshots → risque OOM en self-host. | **C10** | Open | Durcissement prod ; sémaphore 2-3 captures. |
| **F6** | LRU CSS sans plafond total d'octets (worst case ~150 Mo). | **C4** | **Closed** (2026-07-03) | Plafond 8 Mo cumulés + éviction oldest-first + refus des fichiers plus gros que le plafond ; testé (éviction vérifiée par re-fetch). |
| **F7** | Support proxy Chromium ajouté pendant C1 sans amendement de spec : (a) entorse de processus actée ; (b) `bypass` ignorait `NO_PROXY` de l'environnement. | **C2 (début)** | **Closed** (2026-07-03) | Correctif appliqué en 2a : la liste de bypass reprend `NO_PROXY`/`no_proxy`. Le « fallback direct si proxy échoue » envisagé en revue est **rejeté** : ce serait un fallback silencieux (violation charte) — une navigation qui échoue doit le dire, pas changer de chemin réseau. |
| **F8** | Lazy-load non re-déclenché après resize (scroll complet à 1440 uniquement) : hauteurs sous-le-fold potentiellement fausses à 390/768. | **C6** | **Closed** (2026-07-04, 6c) | Re-scroll complet à chaque viewport après resize (le lazy-load spécifique à la largeur monte avant mesure) ; coût ~1-2 s/viewport dans le budget Tier 2 ; audit Chromium vert. |
| **F9** | `CAPTURE_VIEWPORTS` n'inclut pas 320 alors que le plancher qualité audite 320/390/768/1440. | **C10** | Open | Élargissement de l'union au moment où VERIFY en a besoin (trivial, additif). |
| **F10** | `adoptedStyleSheets` / CSSOM construits invisibles pour `discoverFromHtml`. Framer/Webflow non concernés ; angle mort sur sites custom modernes. | **C4** | **Closed** (2026-07-03, 4b) | Dump du CSSOM vivant (`document.styleSheets` + `adoptedStyleSheets`) dans le snapshot, caps 40/256 Ko/1 Mo, feuilles cross-origin comptées ; `RenderedSite.runtimeCss` additif (`via:"runtime"`). |
| **F11** | Dette légère : `BROWSER_UA` dupliqué (engine.ts / fetch-css.ts) ; `staticFetchWith` sans en-têtes UA (seam de test) ; `IMPORT_RE` peut matcher un `@import` commenté (faux « partial »). | **C4** | **Closed** (2026-07-03) | `BROWSER_UA` exporté d'engine.ts (source unique) ; seam aligné sur les en-têtes de `fetchStatic` ; commentaires CSS strippés avant le scan `@import` (contenu stocké verbatim) ; testé. |
| **F12** | `quality.css:"none"` confond « site sans CSS » et « échec de collecte » ; fallback famille de `mergeFonts` peut rattacher le `src` d'une autre graisse. | **C4** | **Closed** (2026-07-03) | Note explicite « page declares no stylesheets » quand le HTML n'en déclare aucun ; `mergeFonts` exige famille + style + graisse couvrante (plage variable acceptée) ; testé. |

## Findings de la revue du Chantier 3 (revue Principal Engineer, 2026-07-03)

| ID | Description | Chantier prévu | Statut | Justification du report |
|----|-------------|----------------|--------|--------------------------|
| **F13** | Heuristique de langue : les langues voisines non listées peuvent être mal classées (pt→es, nl→de) → libellés générés dans la mauvaise langue quand `<html lang>` est absent. | **C6** | Open | Ajout pt/nl + marge contre « inconnu » à calibrer avec la mesure par scène. |
| **F14** | Le planner insère toujours un slot FAQ + une recommendation « Added an FAQ » alors que le composer omet désormais la section sans données réelles → la recommendation décrit une section inexistante (trace mensongère). | **C6** | **Closed** (2026-07-04, 6d) | `planSmart(opts.hasFaq)` : sans FAQ réelle, ni slot (flow, canonique et preserve) ni recommendation « Added an FAQ » ; `directFaqLayout(hasFaq)` sans `\|\| true` ; appelants legacy sans opts inchangés (compat V5 testée). |
| **F15** | La langue et les headings réels ne passent pas par `Sourced<T>` : la provenance du contenu (réel vs libellé généré) n'apparaît pas dans la PipelineTrace. | **C6/C7** | **Closed** (2026-07-04, 6d) | `dna/content-trace.ts` : langue, headings par catégorie et copy CTA tracés dans la PipelineTrace via resolveField (choisi/rejeté/raison), miroir exact de l'arbitrage du composer (realHeading \|\| label). |
| **F16** | `extractPrimaryCtaLabel` : montée d'ancêtres jusqu'à un conteneur large possible (scan de page entière sur DOM plat) ; un lien inline peut gagner sur le vrai bouton. | **C6** | **Closed** (2026-07-04, 6d) | `measure/scenes.ts#heroCtaLabel` : CTA du hero mesuré par styles calculés + géométrie, prime la règle DOM (qui reste le fallback Tier 1) via `analyzeUrlV2WithCapture`. |
| **F17** | Overflow horizontal flaky sur `/zpreview?industry=restaurant&img=1` @768px : `document.scrollWidth-clientWidth` oscille entre 7 et 440px selon le timing (préexistant — reproduit à l'identique sur la baseline SANS C7a, DOM des wrappers prouvé identique). Piste : marquee/carousel mesuré pré-hydratation + images externes bloquées (E1). | **C11** (Quality Gate, ex-C10 — renumérotation D6) | Open (2026-07-10) | La mesure d'overflow doit attendre la stabilisation (animations/hydratation) ; à corriger avec le harnais Quality Gate, pas dans le renderer. |
| **F19** | **Le parcours produit par défaut n'exécute pas le moteur V2** : dashboard démarre en mode `preserve` (`dashboard/page.tsx:64`) → `useLegacy=true` (`generate-site/route.ts:52`) → `generateSite` legacy (pas de DNA/resolver/tokens/SceneSpec, `defaultFaq` fabriquée `engine.ts:2022`, libellés EN). Tout C1→C7 invisible pour le client. Cause n°1 de « l'architecture progresse plus vite que la qualité perçue ». | **P0** | **Closed** (2026-07-10, P0) | Dashboard défaut → smart (recommended) ; preuve avant/après harnais : moteur DNA sur 9/9 reconstructions. |
| **F20** | **Le dashboard bypasse les mesures C4→C6 même en smart** : la route n'appelle `enrichWithMeasurements` que sur `url` seul (`route.ts:31-41`) ; le dashboard poste `{analysis, mode}` (`dashboard/page.tsx:131`) → D4 inopérant dans le parcours produit. | **P0** | **Closed** (2026-07-10, P0) | La route mesure aussi sur `body.analysis` (best-effort, gate navigateur). En sandbox E1 empêche la capture externe ; mécanisme actif en prod. |
| **F21** | **Fabrication de services préséts** (violation règle d'or) : `bridge.ts:61` injecte `profile.defaults.services` (anglais) dans `extractedContent` ; composer L277/356 même fallback → « Tax planning » sur une agence immobilière, « Automation » sur 212 hôpitaux, rendus jusqu'à ×3/page. | **P0** | **Closed** (2026-07-10, P0) | bridge sans préséts ; composer/legacy omettent sans réel ; defaultFaq supprimé ; AI-edit refuse d'inventer. Avant/après : 9/9 → 0/9. Le fallback DIVULGUÉ (site injoignable → « sensible template », l'utilisateur édite) est conservé : template déclaré ≠ fabrication silencieuse. |
| **F22** | **Industrie fausse 4/9 sur sites réels** (immobilier→finance, architecture→realestate, équipement pro→realestate, hôpitaux→saas) → badge/CTA/préséts/flow hérités faux (« Start free » sur ELSAN). Classification par mots-clés de headings insuffisante ; les signaux métier massifs du DOM (458 cartes produit, 708 « room », 306 « booking ») ne sont pas utilisés. | **C8** | Open (2026-07-10) | `eval-c7e/dumps/*.json` (analysis.industry vs scan source). |
| **F23** | L'extraction FAQ capte les bannières cookies (« Fonctionnel Toujours activé », « Préférences »… pris pour des questions) — du bruit RGPD présenté comme contenu réel. | **C8** | Open (2026-07-10, P0) | Vu sur bouillon-chartier ; l'omission du bloc (plan) a masqué le bruit, mais l'extraction doit filtrer les consent walls. |
| **F24** | **Le chemin smart rendait des pages vides de texte** : le composer émettait `headline`/`sectionTitle`, clés lues par AUCUN skin (`props.sectionTitle` : 0 lecture ; `props.title` : 57) → h1/h2/CTA vides sur toute reconstruction smart. Invisible avant P0 (produit=legacy, zpreview=legacy). | **P0** | **Closed** (2026-07-10, P0) | Alias canoniques émis par le composer (hero+7 sections+cta) ; preuve DOM avant (h1 vide) / après (texte réel). Leçon : le chemin smart n'avait JAMAIS été regardé visuellement — désormais impossible (D8). |
| **F25** | `data-animate="off"` ne neutralisait pas `filter: blur` des entrances blur-fade → hero FLOU sur les rendus statiques (défaut des sites publiés). | **P0** | **Closed** (2026-07-10, P0) | Règle CSS `filter:none` ajoutée ; capture elsan avant (floue) / après (nette). |
| **F18** | **E-commerce reconstruit en vitrine** : ReFrame applique une logique de site vitrine aux e-commerces — pas de compréhension collections/catégories/produits/variantes/prix/promos/stock/avis/panier/paiement/wishlist/cross-sell/recherche/filtres ; fiches produits non générées ; CTA génériques au lieu d'« Ajouter au panier »/« Choisir une variante ». Constat utilisateur (2026-07-10) sur reconstructions réelles. | **C8** (Business Understanding Engine) | Open (2026-07-10) | Le plus gros manque produit actuel ; nécessite la compréhension métier (D6), pas un patch du composer. |

## Déviations de spec actées

| ID | Description | Décision |
|----|-------------|----------|
| D13 | **DESIGN OVERHAUL V3 — identité MONOCHROME intemporelle** (2026-07-13, supersede D12). Revirement : l'indigo de D12 est à son tour abandonné. Aucune couleur ne domine — ni vert, ni bleu, ni violet. Grayscale PUR : canvas near-black NEUTRE (`--background 0 0% 5.5%` ≈ #0E0E0E, plus de teinte cool), texte off-white (`--foreground 0 0% 98%`), l'accent **n'est plus une teinte** mais le clair (`--accent 0 0% 96%`, `--primary 0 0% 100%`, `--ring 0 0% 64%`). Glass V3 : `blur(24px)`, border blanc `.08`, radius 24px, ombre très légère, JAMAIS de glow/halo/ombre colorée (`.glass/.glass-dark/.panel/.ambient` neutralisés). Statut : succès/publié neutralisé (check + surface blanche translucide, fin du vert) ; scores lus par la clarté (lightness) pas la teinte ; warning/erreur (ambre/rouge) restent des signaux fonctionnels restreints. Changement de TOKENS (`globals.css`) + résidus lime landing (compare) + commentaires. Tout le chrome hérite (D11). Sites générés inchangés (brand-agnostiques). Critère : « pourrait apparaître dans une keynote Apple ». **Précisé (Creative Director, palette exacte)** : fond `#080808`, sidebar `#101010`, surface `#151515`, hover `#1B1B1B`, texte `#FAFAFA`/`#CFCFCF`/`#8E8E8E`, bordures `rgba(255,255,255,.08→.16)` ; accent = argent `#F3F3F3` (bouton `#F5F5F5`/`#090909`), parcimonie absolue. Radius V3 : 3 crans (12 contrôles / **16 boutons** `rounded-2xl`, fin des pills / **24 cartes** `rounded-3xl`=`.glass`). Couleurs d'état sanctionnées, FONCTION uniquement jamais décoratives : `--success #22C55E`, `--warning #F59E0B`, `--destructive #EF4444`, `--info #3B82F6` (→ `bg-success` etc.). Inspiration : 40% Lovable · 25% Apple · 20% Linear · 15% Framer. Détail de marque : reflet argenté / lueur blanc-froid subtile, jamais néon. | Actée (2026-07-13). |
| D12 | **Identité premium — abandon du « vert IA »** (2026-07-10, sprint Premium Edition, PX1). ⚠️ SUPERSÉDÉE par D13 (l'indigo est abandonné au profit du monochrome pur). Nouvelle identité intemporelle Apple/Linear/Framer : accent **indigo** (`--accent 234 78% 54%`, AA 7.0 sur blanc, remplace le lime), canvas near-black faintement cool (`--background 232 16% 5%`, fin du noir pur), brand **violet discret** (`--brand 256 72% 66%`, usage rare), off-white. Changement de TOKENS uniquement (`globals.css`) → tout le chrome hérite (D11) sans régression. Verre `.glass/.glass-dark/.panel` conservés (héritent de l'accent). Verts sémantiques (succès/publié) restent distincts. Sites générés inchangés (brand-agnostiques). | Actée (2026-07-10). |
| D11 | **Design System du chrome de l'app gelé** (2026-07-10, UX5). Tokens uniques pour le chrome ReFrame (distinct de `DESIGN.md` qui régit les SITES générés) : rayons (`--radius` 12px, échelle sm8/md10/lg12/pill), motion (`ease-premium` cubic-bezier(.2,.8,.2,1) + `duration-fast`120/`duration-base`180), ombres légères (`shadow-raise`/`shadow-float`/`shadow-hairline`, plus de shadow-2xl), quasi-monochrome (accent = guide seulement). Source : `globals.css`+`tailwind.config.ts`. Doc gelée : `docs/DESIGN_SYSTEM.md`. Règle : toute UI de chrome consomme ces tokens, aucune valeur ad hoc ; étendre = éditer les 2 sources + le doc + décision au registre. | Actée (2026-07-10). |
| U0 | **Progrès honnête — no-fabrication appliquée au PRODUIT** (verrouillé 2026-07-10, GO UX1). Le flux de publication (et toute UI produit) ne théâtralise jamais une capacité non exécutée : pas d'animation « SSL généré » / « déploiement CDN » si le backend ne le fait pas — soit on l'implémente, soit on l'omet, soit on la marque « bientôt » (bouton désactivé, jamais mort). Miroir de D9/R0 côté interface. Corollaire mesuré : les métriques de qualité UX ne reportent que des mesures robustes (UX1 a écarté « dominance % » non fiable). | Verrouillée (2026-07-10) — `docs/UX_WORKSPACE_PUBLISH_SPEC.md` §2, UX1. |
| D10 | **`docs/REASONING.md` est le modèle mental fondateur du moteur** (2026-07-10). Le raisonnement de ReFrame est formalisé en 4 actes / 12 étapes (R1 observer · R2-R7 comprendre : identité/langage visuel/offre/modèle/capacités/buts+parcours · R8-R9 juger : indispensable/améliorable · R10-R12 agir & prouver : composer l'expérience/vérifier la préservation/vérifier la crédibilité). **Le raisonnement est le référentiel d'appartenance des modules** (L5) : tout module — présent ou futur — déclare « à quelle étape R j'interviens » AVANT d'être codé, jamais « à quel fichier j'appartiens ». Lois transverses L1-L6 (causalité stricte, jugement=affirmation+preuves+confiance, l'inconnu se déclare, tout se justifie via la trace, le raisonnement est le référentiel, la preuve est externe/harnais). Distinction fondatrice : le moteur produit de la COMPRÉHENSION (affirmation falsifiable, sourcée, lourde de conséquences), pas de la donnée. | Actée (2026-07-10) — `docs/REASONING.md`. |
| D9 | **Règle absolue : raisonner en expert métier** (2026-07-10, prime sur toutes les autres). ReFrame ne raisonne plus en pages/sections/composants mais comme un expert qui comprend l'entreprise : qui elle est, ce qu'elle vend, comment elle gagne de l'argent, ce qu'elle attend du visiteur, le parcours qui y mène. **Gate de composition** : le Composition Engine ne compose pas tant que les questions métier (identité, objets, parcours, CTA critiques, capacités à préserver, pertes inacceptables) n'ont pas de réponse — ou d'INCONNU déclaré et tracé (un site pauvre en signaux se reconstruit avec dégradation honnête, jamais avec invention). La préservation s'étend aux MODÈLES ÉCONOMIQUES : objets, capacités, parcours, intentions, points de conversion — ce qui fait vivre l'entreprise. Le design premium reste indispensable mais au service du business, jamais l'inverse. Clôture C8 : le moteur explique chaque décision de composition depuis la compréhension métier (trace). | Actée (2026-07-10) — spec v2 + `docs/C8_IMPLEMENTATION_PLAN.md`. |
| D8 | **La qualité perçue devient le critère de validation principal** (2026-07-10). Un sous-lot n'est réussi que si, en plus de tests verts/tsc/fallbacks, il démontre une amélioration visuelle/fonctionnelle perceptible sur plusieurs sites réels (harnais `scripts/c7e-audit.mjs`, avant/après). Audit fondateur : `docs/C8_PREPARATION.md` (M1-M12, 44 défauts, carte des fuites). | Actée (2026-07-10) — inscrite au protocole CONVENTIONS. |
| D7 | **Compréhension multi-couches + Intent Engine** (vision actée 2026-07-10, extension de D6). À terme le Composition Engine fusionne PLUSIEURS couches de compréhension complémentaires (BrandDNA, BusinessDNA, ContentDNA, SceneDNA, DesignDNA, MotionDNA, ResponsiveDNA, QualityDNA) **sans jamais connaître leur origine** — il ne reçoit que des décisions résolues avec provenance. Un **Intent Engine** (« pourquoi cette section existe : rassurer/vendre/convertir/réserver/démontrer l'expertise/générer un appel ») complétera le Business Understanding (« quel est ce site »), pour composer selon l'OBJECTIF de la section, pas seulement son type. Règles absolues : le renderer ne prend jamais de décision métier ; le Composition Engine ne contient jamais de logique spécifique à une industrie ; toute intelligence arrive sous forme de données. Encodé dès C7d : `SceneSpecSources` = sources NOMMÉES fusionnées fill-only par rang — toute nouvelle couche s'ajoute comme entrée sans refonte. | Actée (2026-07-10). |
| D6 | **Business Understanding → Composition, jamais l'inverse** (vision actée 2026-07-10). Le pipeline cible insère un **Business Understanding Engine** (C8) AVANT la composition : industrie exacte, sous-catégorie, modèle économique, objectifs, parcours critiques, objets métier (biens/chambres/menu/produits/variantes/prix…), actions clés, contenus prioritaires, signaux de confiance. Implications immédiates pour C7 : (a) le Composition Engine reste **générique** — il consomme des couches de décisions (`Sourced`, fill-only, mêmes rangs I1/A2) et devra recevoir demain une **BusinessDNA** exactement comme SceneDNA/DesignDNA (nouvelle couche `CandidateLayer` + nouveaux champs de `SceneSpec`, zéro refonte) ; (b) découplage fort : aucune logique métier dans `compose/`/renderer — l'intelligence sectorielle s'injecte par données (couches), pas par branchements dans les skins ; (c) `buildContentModel`/`business.ts` actuels = précurseurs à absorber par C8, ne pas les étendre ad hoc pendant C7. | Actée (2026-07-10) — guide toutes les décisions C7c→C7e. |
| D1 | `capturedAt` : la spec C1 le disait « injecté par l'appelant » ; implémenté avec défaut interne `new Date().toISOString()` + override (`CaptureInternals.capturedAt`). | Acceptée (2026-07-03) — sans conséquence, documentée dans le code. |
| D2 | `server/browser.ts` : support `HTTPS_PROXY` non prévu par la spec C1, ajouté pendant le debug de la démo réelle. | Acceptée a posteriori — voir F7 ; tout ajout hors spec doit désormais passer par ce registre AVANT le code. |
| D5 | **Reference Learning Engine branché** (avance du Chantier 8) : `findClosestReferences(visualDna, ENRICHED_REFERENCE_DB)` → `inspirationLayer` = couche `curated` dérivée des **décisions mesurables** de la référence premium la plus proche (Archform), gated `strongestMatch >= 0.6`. Aucune copie de markup (règle charte respectée). I1 : les mesures du site source gagnent ; l'inspiration comble les trous ; au-dessus du moodboard catégoriel (curated 0.5), sous les mesures. Diversité préservée (couche muette hors match). Actuellement seule Archform a un `richDna` → enrichir la bibliothèque (autres profils premium mesurés) étend la couverture sans autre changement de pipeline. | Actée (2026-07-04). |
| D4 | **Raccordement production C4→C6** (sous-lot dédié) : `route.ts` appelle `enrichWithMeasurements` après `analyzeUrlV2`. Quand un navigateur rend la source, une capture Tier 2 est faite **en plus** de l'extraction (double rendu) → +5-10 s/site et Chromium requis en prod. Gardé par disponibilité navigateur + kill-switch `REFRAME_MEASURE=0` ; dégradation octet-identique sinon. | Actée (2026-07-04). Optimisation future (replier la capture dans `analyzeUrlV2` pour un seul rendu) = candidat C11 ; coût prod à valider au déploiement (cf. F5/E1). |
| D3 | Chantier 3 : trois changements **délibérés** du comportement par défaut, mandatés par les objectifs du chantier et la règle d'or no-fabrication : (a) la FAQ n'est plus jamais fabriquée (`defaultFaq` supprimé — section omise sans données réelles, comme testimonials/stats) ; (b) le contenu réel prime (headings de sections, copy CTA, headline du CTA de clôture) ; (c) les libellés générés suivent la langue détectée (en/fr/es/de/it, anglais octet-identique à V5 en fallback). La contrainte « aucun changement du comportement V5 par défaut » est interprétée comme : API/architecture V5 intactes, moteur legacy intact, consommateurs non cassés. | Actée (2026-07-03), tests mis à jour en conséquence. |

## Limitations d'environnement connues

| ID | Description | Impact |
|----|-------------|--------|
| E1 | La sandbox de développement tue les handshakes TLS des navigateurs vers l'extérieur (CONNECT 200 puis reset, y compris via le Playwright MCP du harness). | Le Tier 2 sur sites réels n'est vérifiable qu'en prod (C10) ; validé localement via la fixture Chromium. Les dégradations sont tracées dans `quality.notes`. |

## Chantier 2 — décisions d'architecture

| ID | Description | Décision |
|----|-------------|----------|
| A1 | Monotonie stricte de `refine()` : aucun slot occupé n'est jamais réécrit, par aucune source. La précision tardive s'exprime par un champ plus spécifique (`heightRange` → `heightPx`), jamais par réécriture (principe #9 validé). | Actée — démonstration formelle G1-G4 livrée avant implémentation (session 2026-07-03). |
| A2 | Règle charte « un curated ne bat un measured que si confidence(measured) < 0.4 » : encodée dans la fonction de rang du resolver (rétrogradation du measured faible), le measured rétrogradé restant archivé dans `rejected`. | Actée. |
| A3 | Conservation totale : tout candidat offert est soit `chosen`, soit dans `rejected[]` ; invariant testé `|offerts| = 1 + |rejected|`. Les entrées du resolver sont gelées profondément. | Actée. |
